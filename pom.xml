<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

	<modelVersion>4.0.0</modelVersion>
	<groupId>org.terems</groupId>
	<artifactId>terems-org</artifactId>
	<packaging>pom</packaging>
	<version>0.9.PEDESIS-SNAPSHOT</version>
	<name>Terem's .org</name>
	<description>parent project for app(s) and modules</description>
	<organization>
		<name>Terem's .org</name>
		<url>http://terems.org</url>
	</organization>

<!-- TODO stop using Jetty before making WebZ available under GPL (otherwise it can only be either EPL or Apache License) !!! -->

	<modules>

		<module>webz-api</module>
		<module>webz</module>
		<module>webz-default-plugins</module>
		<module>webz-dropbox</module>
		<module>webz-deployable</module>

		<module>webz-test-utils</module>

	</modules>

	<properties>

		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

		<jetty.http.port>8080</jetty.http.port>
		<jetty.stop.port>8079</jetty.stop.port>
		<jetty.stop.key>jetty-webz</jetty.stop.key>
		<jetty.stop.wait.milliseconds>5000</jetty.stop.wait.milliseconds>
		<jetty.sleep.after.stop.milliseconds>100</jetty.sleep.after.stop.milliseconds>

		<!-- versions -->

		<jetty.version>9.2.6.v20141205</jetty.version>
		<servlet.api.version>3.0.1</servlet.api.version><!-- 3.1.0 -->

		<dropbox.version>[1.7,1.8)</dropbox.version>
		<ehcache.version>2.8.3</ehcache.version>
		<jgit.version>3.6.1.201501031845-r</jgit.version>

		<commons.codec.version>1.9</commons.codec.version>
		<commons.io.version>2.4</commons.io.version>
		<commons.lang3.version>3.3.2</commons.lang3.version>

		<guava.version>15.0.0.v201403281430</guava.version><!-- 17.0 -->
		<wikitext.version>2.2.0.N20140702-2138</wikitext.version>

		<slf4j.log4j12.version>1.7.7</slf4j.log4j12.version>
		<log4j.version>1.2.17</log4j.version>

		<maven.compiler.plugin.version>3.1</maven.compiler.plugin.version>
		<maven.eclipse.plugin.version>2.9</maven.eclipse.plugin.version>
		<maven.war.plugin.version>2.4</maven.war.plugin.version>

		<maven.antrun.plugin.version>1.7</maven.antrun.plugin.version>

		<testng.version>6.8.8</testng.version>
		<mockito.version>1.9.5</mockito.version>

		<javassist.version>3.18.2-GA</javassist.version>
		<objenesis.version>2.1</objenesis.version>

	</properties>

	<dependencyManagement>
		<dependencies>

			<dependency>
				<groupId>${project.groupId}</groupId>
				<artifactId>webz-api</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>${project.groupId}</groupId>
				<artifactId>webz</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>${project.groupId}</groupId>
				<artifactId>webz-default-plugins</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>${project.groupId}</groupId>
				<artifactId>webz-dropbox</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>${project.groupId}</groupId>
				<artifactId>webz-deployable</artifactId>
				<version>${project.version}</version>
			</dependency>

			<!-- Servlet API -->
			<dependency>
				<groupId>javax.servlet</groupId>
				<artifactId>javax.servlet-api</artifactId>
				<version>${servlet.api.version}</version>
				<scope>provided</scope>
			</dependency>

			<!-- Apache -->
			<dependency>
				<groupId>commons-io</groupId>
				<artifactId>commons-io</artifactId>
				<version>${commons.io.version}</version>
			</dependency>
			<!--
			<dependency>
				<groupId>commons-codec</groupId>
				<artifactId>commons-codec</artifactId>
				<version>${commons.codec.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.commons</groupId>
				<artifactId>commons-lang3</artifactId>
				<version>${commons.lang3.version}</version>
			</dependency>
			-->

			<!-- Dropbox -->
			<dependency>
				<groupId>com.dropbox.core</groupId>
				<artifactId>dropbox-core-sdk</artifactId>
				<version>${dropbox.version}</version>
			</dependency>

			<!-- Ehcache -->
			<dependency>
				<groupId>net.sf.ehcache</groupId>
				<artifactId>ehcache</artifactId>
				<version>${ehcache.version}</version>
			</dependency>

			<!-- JGit -->
			<dependency>
				<groupId>org.eclipse.jgit</groupId>
				<artifactId>org.eclipse.jgit</artifactId>
				<version>${jgit.version}</version>
			</dependency>

			<!-- Mylyn WikiText -->
			<!--
			<dependency>
			    <groupId>com.google.guava</groupId>
			    <artifactId>guava</artifactId>
			    <version>${guava.version}</version>
			</dependency>
			<!- ! ->
			<dependency>
				<groupId>org.eclipse.mylyn</groupId>
				<artifactId>wikitext.core</artifactId>
				<version>${wikitext.version}</version>
			</dependency>
			<dependency>
				<groupId>org.eclipse.mylyn</groupId>
				<artifactId>wikitext.confluence.core</artifactId>
				<version>${wikitext.version}</version>
			</dependency>
			<dependency>
				<groupId>org.eclipse.mylyn</groupId>
				<artifactId>wikitext.html.core</artifactId>
				<version>${wikitext.version}</version>
			</dependency>
			<dependency>
				<groupId>org.eclipse.mylyn</groupId>
				<artifactId>wikitext.markdown.core</artifactId>
				<version>${wikitext.version}</version>
			</dependency>
			<dependency>
				<groupId>org.eclipse.mylyn</groupId>
				<artifactId>wikitext.mediawiki.core</artifactId>
				<version>${wikitext.version}</version>
			</dependency>
			<dependency>
				<groupId>org.eclipse.mylyn</groupId>
				<artifactId>wikitext.textile.core</artifactId>
				<version>${wikitext.version}</version>
			</dependency>
			<dependency>
				<groupId>org.eclipse.mylyn</groupId>
				<artifactId>wikitext.tracwiki.core</artifactId>
				<version>${wikitext.version}</version>
			</dependency>
			<dependency>
				<groupId>org.eclipse.mylyn</groupId>
				<artifactId>wikitext.twiki.core</artifactId>
				<version>${wikitext.version}</version>
			</dependency>
			-->

			<!-- Logging -->
			<dependency>
				<groupId>org.slf4j</groupId>
				<artifactId>slf4j-log4j12</artifactId>
				<version>${slf4j.log4j12.version}</version>
			</dependency>
			<dependency>
				<groupId>log4j</groupId>
				<artifactId>log4j</artifactId>
				<version>${log4j.version}</version>
			</dependency>

			<!-- Jetty (for the sake of viewing it's source code) -->
			<dependency>
				<groupId>org.eclipse.jetty</groupId>
				<artifactId>jetty-ant</artifactId>
				<version>${jetty.version}</version>
				<scope>provided</scope>
			</dependency>
			<!--
			<dependency>
				<groupId>org.eclipse.jetty.aggregate</groupId>
				<artifactId>jetty-all</artifactId>
				<version>${jetty.version}</version>
				<scope>provided</scope>
			</dependency>
			-->

			<!-- Testing -->
			<dependency>
				<groupId>org.testng</groupId>
				<artifactId>testng</artifactId>
				<version>${testng.version}</version>
				<scope>test</scope>
			</dependency>
			<dependency>
				<groupId>org.mockito</groupId>
				<artifactId>mockito-all</artifactId>
				<version>${mockito.version}</version> 
				<scope>test</scope>
			</dependency>

			<dependency>
				<groupId>${project.groupId}</groupId>
				<artifactId>webz-test-utils</artifactId>
				<version>${project.version}</version>
				<scope>test</scope>
			</dependency>

			<dependency>
				<groupId>org.javassist</groupId>
				<artifactId>javassist</artifactId>
				<version>${javassist.version}</version>
			</dependency>
			<dependency>
				<groupId>org.objenesis</groupId>
				<artifactId>objenesis</artifactId>
				<version>${objenesis.version}</version>
			</dependency>

		</dependencies>
	</dependencyManagement>

	<build>
		<resources>
			<resource>
				<filtering>true</filtering>
				<directory>src/main/resources</directory>
			</resource>
			<resource>
				<filtering>false</filtering>
				<directory>src/main/java</directory>
				<includes>
					<include>**</include>
				</includes>
				<excludes>
					<exclude>**/*.java</exclude>
				</excludes>
			</resource>
		</resources>
		<pluginManagement>
			<plugins>
				<plugin>
					<inherited>true</inherited>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>${maven.compiler.plugin.version}</version>
					<configuration>
						<source>1.6</source>
						<target>1.6</target>
						<encoding>UTF-8</encoding>
						<showWarnings>true</showWarnings>
						<showDeprecation>true</showDeprecation>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-eclipse-plugin</artifactId>
					<version>${maven.eclipse.plugin.version}</version>
					<configuration>
						<downloadSources>true</downloadSources>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-war-plugin</artifactId>
					<version>${maven.war.plugin.version}</version>
					<configuration>
				        <webResources>
				            <resource>
				                <filtering>true</filtering>
				                <directory>src/main/webapp</directory>
				                <includes>
				                    <include>**/*.xml</include>
				                </includes>
				            </resource>
				        </webResources>
				        <warSourceDirectory>src/main/webapp</warSourceDirectory>
				        <webXml>src/main/webapp/WEB-INF/web.xml</webXml>
						<!--
						<failOnMissingWebXml>false</failOnMissingWebXml>
						-->
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-antrun-plugin</artifactId>
					<version>${maven.antrun.plugin.version}</version>
					<dependencies>
						<dependency>
							<groupId>org.eclipse.jetty</groupId>
							<artifactId>jetty-ant</artifactId>
							<version>${jetty.version}</version>
							<exclusions>
								<exclusion>
									<groupId>ant</groupId>
									<artifactId>ant</artifactId>
								</exclusion>
								<exclusion>
									<groupId>ant</groupId>
									<artifactId>ant-launcher</artifactId>
								</exclusion>
							</exclusions>
						</dependency>
					</dependencies>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>
</project>